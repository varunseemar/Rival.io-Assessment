# Render (and other PaaS) set NODE_ENV=production, which makes `npm install`
# skip devDependencies. The build needs them (typescript, @types/*), so force
# their inclusion. Runtime still only requires the production dependencies.
include=dev
